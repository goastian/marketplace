## Midori Marketplace Environment

```bash
APP_URL=http://localhost
VITE_MARKETPLACE_API_BASE_URL=${APP_URL}
```

Usa aquí el origen del marketplace; el cliente de la extensión resuelve `/api/v1` automáticamente.
